import { FlightRoutePipe } from './flight-route.pipe';

describe('FlightRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
