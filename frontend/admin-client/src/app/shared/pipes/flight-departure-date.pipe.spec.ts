import { FlightDepartureDatePipe } from './flight-departure-date.pipe';

describe('FlightDepartureDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightDepartureDatePipe();
    expect(pipe).toBeTruthy();
  });
});
